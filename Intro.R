# Script

hectare <- 10
hectare <- 12
acre <- hectare*2.47

#Vector
members <- c(3,4,5,7)
sqrt(members)
sum(members)  
mean(members)
median(members)
words <- c("hello", "goodbye", "yo")
class(members)
round(words)

#Main vector types (classes): double, integer, character, logical


